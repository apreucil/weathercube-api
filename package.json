{
  "name": "weatherapi",
  "version": "1.0.0",
  "description": "This is a dummy description",
  "scripts": {
    "build-watch": "parcel watch ./client/index.html",
    "start-watch": "NODE_ENV=dev nodemon server/index.js",
    "dev": "concurrently --kill-others \"npm:start-watch\" \"npm:build-watch\"",
    "build": "parcel build ./client/index.html",
    "start": "npm run build && NODE_ENV=production node server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.20",
    "parcel": "^2.8.0"
  }
}
